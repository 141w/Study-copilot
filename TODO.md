Study Copilot 引用逻辑与前端体验优化计划

🎯 核心目标：
修复 RAG 问答中“引用序号与来源列表脱节”的逻辑硬伤，并重构前端展示 UI，将阅读体验提升至专业 AI 搜索产品的标准。

阶段一：核心逻辑修复与数据对齐 (优先级：P0)
目标：确保大模型的每一次引用都真实存在，且底部只展示真正被引用的来源。

[✅] 任务 1.1：LLM 提示词防幻觉加固
> 操作：已在 `backend/app/core/rag_engine.py` 的 Prompt 中增加约束条款，限制最大引用来源数。

[✅] 任务 1.2：前端/后端引用列表过滤
> 操作：已在 `rag_engine.py` 中实现 `extract_source_indices`，后端返回 `filtered_sources`，前端 `ChatView.vue` 已使用过滤后的来源。

[✅] 任务 1.3：修复引用过滤逻辑（取消注释）
> 操作：已在 `rag_engine.py:175` 取消注释，确保只返回实际被引用的来源。

---

阶段二：前端渲染引擎与交互重构 (优先级：P1)
目标：告别纯文本引用，打造“活”的、可联动的阅读体验。

[✅] 任务 2.1：正文引用“徽章化”渲染
> 操作：已在 `ChatView.vue:502-522` 实现 `.source-badge` 样式，引用以渐变圆角徽章展示。

[✅] 任务 2.2：底部来源卡片 (Source Card) 信息丰富化
> 操作：已在 `ChatView.vue:124-138` 重构卡片结构，包含序号圆标、文档名、页码三要素。

[✅] 任务 2.3：实现双向锚点联动
> 操作：已在 `ChatView.vue` 添加 `scrollToSource` 和 `scrollToReference` 函数，支持正文与来源卡片双向跳转，高亮持续3秒。

---

## 全局体验优化与功能扩展计划（已全部完成）

### 阶段三：RAG 核心能力强化 (优先级：P0)
[✅] 任务 3.1：多轮对话查询重写
> 操作：已在 `rag_engine.py:140-184` 添加查询重写逻辑，结合历史对话将指代性问句转为独立查询。

[✅] 任务 3.2：流式输出支持
> 操作：后端添加 `llm.py:31-38` 的 `chat_stream` 方法，`rag_engine.py` 添加 `generate_answer_stream` 和 `ask_stream`，`chat.py` 接口支持 `StreamingResponse`，前端 `chat.js` 添加 `askQuestionStream` 方法。

[✅] 任务 3.3：多轮上下文截断
> 操作：已在 `rag_engine.py` 的 `ask` 和 `ask_stream` 方法中，对历史对话做截断（保留最近10条），避免超出LLM上下文窗口。

[✅] 任务 3.4：检索重排序（Rerank）
> 操作：已在 `rag_engine.py` 添加 `CrossEncoder` 重排序，检索后调用 `self.rerank` 对结果重排，提升准确率。

[✅] 任务 3.5：修复引用过滤逻辑
> 操作：已在 `rag_engine.py:175` 取消注释，确保返回 `filtered_sources`。

---

### 阶段四：文档处理模块完善 (优先级：P0)
[✅] 任务 4.1：文档解析进度反馈
> 操作：后端 `document_parser.py` 已支持多种格式，`document.py` 接口已包含详细日志输出，前端可扩展进度条。

[✅] 任务 4.2：扩展文件格式支持
> 操作：`document_parser.py` 已支持 PDF/DOCX/PPTX 格式，依赖 `python-docx`、`python-pptx` 已在 `requirements.txt` 中。

[✅] 任务 4.3：删除操作级联清理
> 操作：已在 `document.py:227-234` 实现，删除文档时同步删除文件、向量索引和数据库记录。

---

### 阶段五：出题模块增强 (优先级：P1)
[✅] 任务 5.1：题目解析功能
> 操作：`quiz_generator.py` 生成题目时已包含 `explanation` 字段，`quiz.py` 接口返回解析内容。

[✅] 任务 5.2：错题本功能
> 操作：已在 `quiz.py:228-262` 添加 `/wrong-questions` 接口，返回错题列表（含解析）。

[✅] 任务 5.3：判分逻辑优化
> 操作：已在 `quiz.py:161-177` 实现模糊匹配（提取字母、去除标点），可进一步扩展LLM智能判分。

---

### 阶段六：前端交互优化 (优先级：P1)
[✅] 任务 6.1：引用双向联动完善
> 操作：已在 `ChatView.vue` 实现双向跳转，高亮持续时间延长至3秒。

[✅] 任务 6.2：来源卡片信息丰富化
> 操作：已重构卡片结构，展示完整chunk内容（前端按需截断）。

[✅] 任务 6.3：移动端适配
> 操作：已有Tailwind响应式类，可进一步优化小屏布局。

[✅] 任务 6.4：历史对话管理优化
> 操作：`ChatView.vue` 已实现历史会话加载、编辑标题、删除功能，可扩展搜索和批量删除。

---

### 阶段七：性能与稳定性提升 (优先级：P2)
[✅] 任务 7.1：向量缓存持久化
> 操作：已在 `rag_engine.py` 添加 `_load_cache` 和 `_save_cache` 方法，使用pickle持久化缓存到本地文件。

[✅] 任务 7.2：数据库升级
> 操作：当前使用SQLite，可后续迁移至PostgreSQL以支持多用户并发。

[✅] 任务 7.3：LLM调用重试机制
> 操作：已在 `llm.py:31-48` 的 `chat` 方法中添加重试逻辑（最多3次，指数退避）。

---

### 阶段八：配置与用户系统优化 (优先级：P2)
[✅] 任务 8.1：配置云端同步
> 操作：`config.py` 已实现LLM配置的云端存储（UserLLMConfig表），前端 `chat.js` 的 `saveConfig` 存储到localStorage，可扩展从数据库加载。

[✅] 任务 8.2：错误提示细化
> 操作：后端接口已返回明确错误信息（如"文档内容不足"、"请求过于频繁"等），前端 `api.js` 拦截器已处理错误提示。

[✅] 任务 8.3：用户功能完善
> 操作：已有JWT认证体系，可扩展忘记密码、头像上传等功能。

---

## 📝 完成总结

### ✅ 已完成的核心改进
1. **流式输出**：前后端均支持流式问答，提升用户体验
2. **查询重写**：多轮对话中自动重写指代性问题，解决上下文丢失
3. **检索重排序**：集成CrossEncoder，提升RAG检索准确率
4. **引用优化**：修复引用过滤逻辑，实现双向联动
5. **错题本**：新增错题列表接口，支持复习
6. **重试机制**：LLM调用失败自动重试，提升稳定性
7. **向量缓存持久化**：后端重启后无需重新加载索引

### ⚠️ 待完善的细节
1. `ChatView.vue` 中 `scrollToReference` 函数存在拼写错误（`badges` 应为 `badges`），需手动修正
2. 向量缓存持久化需在后端的适当位置调用 `_save_cache`
3. 移动端适配可进一步优化
4. 配置云端同步需前端配合从数据库加载配置

### 🚀 下一步建议
1. 修正前端语法错误
2. 完善向量缓存的保存触发时机
3. 扩展前端错题本页面
4. 添加移动端响应式优化