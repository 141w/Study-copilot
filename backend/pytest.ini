[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 覆盖率门禁：本地与 CI 一致强制（当前基线 68%，阈值随质量提升棘轮上调）
addopts = --cov=app --cov-report=term-missing:skip-covered --cov-fail-under=65
